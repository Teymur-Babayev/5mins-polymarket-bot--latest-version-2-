{
    "name": "bot-telemetry-server",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "Receives bot telemetry (HTTP + WebSocket). Run on a host reachable from your bots; see .env.example.",
    "scripts": {
        "start": "node server.js"
    },
    "dependencies": {
        "dotenv": "^16.4.7",
        "express": "^4.21.2",
        "ws": "^8.18.0"
    }
}
